# GitHub OAuth App credentials
#
# Define one or more secrets using the following format:
# GITHUB_OAUTH_SECRET_<NAME>=<client_id>|<client_secret>
#
# For example:
# GITHUB_OAUTH_SECRET_MYAPP=your_client_id_here|your_client_secret_here

# Allowed CORS origins (comma-separated), also used for redirect URI validation
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174

# Server port
PORT=3001

# Log level (trace, debug, info, warn, error, fatal)
LOG_LEVEL=info
